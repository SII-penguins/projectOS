interface:
  display_name: "ProjectOS"
  short_description: "Start, organize, resume, or close complex projects"
  default_prompt: "Use $project-os. I will describe a project idea or share an existing project; infer the right workflow, guide me one step at a time, and do not make me choose internal modes."

policy:
  allow_implicit_invocation: true
