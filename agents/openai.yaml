interface:
  display_name: "ProjectOS"
  short_description: "Start, organize, resume, or close complex projects"
  default_prompt: "Use $project-os to plan or reconcile this project and complete the work within my requested scope."

policy:
  allow_implicit_invocation: true
